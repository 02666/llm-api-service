services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    depends_on:
      - redis
    restart: unless-stopped

  # W3 限流/配额会用：按 Key 计数。现在先放进编排练手。
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    restart: unless-stopped
