# 真实配置：复制为 .env 并填写（.env 已被 .gitignore 排除，绝不进 git！）

# 逗号分隔的合法 API Key（调用你服务的人需要带 X-API-Key 头）
API_KEYS=dev-key-123

# 大模型供应商配置（DeepSeek 示例；换 Qwen/vLLM 只改这三行）
LLM_API_KEY=YOUR_ZHIPU_API_KEY
LLM_BASE_URL=https://open.bigmodel.cn/api/anthropic
LLM_MODEL=glm-5.3-flash

# 单次请求超时（秒）
REQUEST_TIMEOUT=60
